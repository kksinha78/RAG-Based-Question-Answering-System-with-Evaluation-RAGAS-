This project implements an end-to-end Retrieval-Augmented Generation (RAG) pipeline using LangChain, OpenAI, and FAISS, and evaluates the system using Ragas metrics.

It demonstrates how to build a question-answering system over custom documents and measure its performance using structured evaluation.
